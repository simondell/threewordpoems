<% if object.errors.any? %>
<section>
  <h3>Do it again!</h3>
  <p>This form contains <%= pluralize object.errors.count, 'error' %>:</p>
  <ul>
      <% object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
  </ul>
</section>
<% end %>