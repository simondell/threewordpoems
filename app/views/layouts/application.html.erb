<!DOCTYPE html>
<html>
<%= render 'shared/head' %>
<body>

	<%= render "shared/site_title" %>

	<div id="sign-posts">
		<%= render "shared/site_sections" %>

		<%= yield :orientation %>
		<%= yield :collection %>
	</div>

	<%= yield %>

	<%= debug params if Rails.env.development? %>

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>
